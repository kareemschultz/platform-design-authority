{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/catalog.product.activated.v1.schema.json",
  "title": "catalog.product.activated.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "catalog.product.activated.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["productId", "previousState", "version"],
          "properties": {
            "productId": {"type": "string"},
            "previousState": {"type": "string", "enum": ["Draft", "Suspended"]},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
