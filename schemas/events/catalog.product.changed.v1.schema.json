{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/catalog.product.changed.v1.schema.json",
  "title": "catalog.product.changed.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "catalog.product.changed.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["productId", "changedFields", "version"],
          "properties": {
            "productId": {"type": "string"},
            "changedFields": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "maxLength": 100}},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
