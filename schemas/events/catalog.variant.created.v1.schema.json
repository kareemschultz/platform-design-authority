{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/catalog.variant.created.v1.schema.json",
  "title": "catalog.variant.created.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "catalog.variant.created.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["productId", "variantId", "version"],
          "properties": {
            "productId": {"type": "string"},
            "variantId": {"type": "string"},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
