{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/commerce.deposit.reconciled.v1.schema.json",
  "title": "commerce.deposit.reconciled.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "commerce.deposit.reconciled.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["depositId", "depositReference", "currency", "amountMinor", "confirmerPartyId"],
          "properties": {
            "depositId": {"type": "string"},
            "depositReference": {"type": "string"},
            "currency": {"type": "string", "pattern": "^[A-Z]{3}$"},
            "amountMinor": {"type": "integer", "exclusiveMinimum": 0},
            "confirmerPartyId": {"type": "string"}
          }
        }
      }
    }
  ]
}
