{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/commerce.exchange.completed.v1.schema.json",
  "title": "commerce.exchange.completed.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "commerce.exchange.completed.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["exchangeId", "returnId", "newSaleId", "registerId", "currency"],
          "properties": {
            "exchangeId": {"type": "string"},
            "returnId": {"type": "string"},
            "newSaleId": {"type": "string"},
            "registerId": {"type": "string"},
            "currency": {"type": "string", "pattern": "^[A-Z]{3}$"},
            "balanceDueMinor": {"type": "integer"}
          }
        }
      }
    }
  ]
}
