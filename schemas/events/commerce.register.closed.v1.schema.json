{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/commerce.register.closed.v1.schema.json",
  "title": "commerce.register.closed.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "commerce.register.closed.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["registerId", "currency", "countedCashMinor", "expectedCashMinor", "varianceMinor", "closerPartyId", "varianceApprovalRequired"],
          "properties": {
            "registerId": {"type": "string"},
            "currency": {"type": "string", "pattern": "^[A-Z]{3}$"},
            "countedCashMinor": {"type": "integer", "minimum": 0},
            "expectedCashMinor": {"type": "integer", "minimum": 0},
            "varianceMinor": {"type": "integer"},
            "closerPartyId": {"type": "string"},
            "varianceApprovalRequired": {"type": "boolean"},
            "varianceApproverPartyId": {"type": ["string", "null"]}
          }
        }
      }
    }
  ]
}
