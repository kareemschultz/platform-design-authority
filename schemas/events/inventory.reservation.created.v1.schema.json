{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/inventory.reservation.created.v1.schema.json",
  "title": "inventory.reservation.created.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "inventory.reservation.created.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["reservationId", "productId", "locationId", "quantity", "unit", "expiresAt"],
          "properties": {
            "reservationId": {"type": "string"},
            "productId": {"type": "string"},
            "locationId": {"type": "string"},
            "quantity": {"type": "string", "pattern": "^(?:0|[1-9][0-9]*)(?:\\.[0-9]{1,6})?$"},
            "unit": {"type": "string"},
            "expiresAt": {"type": "string", "format": "date-time"}
          }
        }
      }
    }
  ]
}
