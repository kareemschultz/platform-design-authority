{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/inventory.reservation.released.v1.schema.json",
  "title": "inventory.reservation.released.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "inventory.reservation.released.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["reservationId", "reason", "releasedAt"],
          "properties": {
            "reservationId": {"type": "string"},
            "reason": {"type": "string", "enum": ["Fulfilled", "Cancelled", "Expired", "Superseded"]},
            "releasedAt": {"type": "string", "format": "date-time"}
          }
        }
      }
    }
  ]
}
