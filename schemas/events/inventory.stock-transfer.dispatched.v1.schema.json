{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/inventory.stock-transfer.dispatched.v1.schema.json",
  "title": "inventory.stock-transfer.dispatched.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "inventory.stock-transfer.dispatched.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["transferId", "sourceLocationId", "destinationLocationId", "movementIds", "version"],
          "properties": {
            "transferId": {"type": "string"},
            "sourceLocationId": {"type": "string"},
            "destinationLocationId": {"type": "string"},
            "movementIds": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
