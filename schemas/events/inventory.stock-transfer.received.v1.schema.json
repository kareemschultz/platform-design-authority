{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/inventory.stock-transfer.received.v1.schema.json",
  "title": "inventory.stock-transfer.received.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "inventory.stock-transfer.received.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["transferId", "receiptId", "destinationLocationId", "movementIds", "state", "version"],
          "properties": {
            "transferId": {"type": "string"},
            "receiptId": {"type": "string"},
            "destinationLocationId": {"type": "string"},
            "movementIds": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
            "state": {"type": "string", "enum": ["PartiallyReceived", "Received", "Exception"]},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
