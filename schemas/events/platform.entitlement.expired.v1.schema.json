{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/platform.entitlement.expired.v1.schema.json",
  "title": "platform.entitlement.expired.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "platform.entitlement.expired.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["entitlementId", "capabilityId", "state", "version"],
          "properties": {
            "entitlementId": {"type": "string"},
            "capabilityId": {"type": "string"},
            "organizationId": {"type": ["string", "null"]},
            "state": {"const": "Expired"},
            "startsAt": {"type": "string", "format": "date-time"},
            "endsAt": {"type": ["string", "null"], "format": "date-time"},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
