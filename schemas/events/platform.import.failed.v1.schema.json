{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/platform.import.failed.v1.schema.json",
  "title": "platform.import.failed.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "platform.import.failed.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["importId", "target", "failureCode", "lastCompletedRow", "version"],
          "properties": {
            "importId": {"type": "string"},
            "target": {"type": "string", "enum": ["Product", "OpeningStock"]},
            "failureCode": {"type": "string", "minLength": 1, "maxLength": 100},
            "lastCompletedRow": {"type": "integer", "minimum": 0, "maximum": 1000},
            "version": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  ]
}
