{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/platform.membership.activated.v1.schema.json",
  "title": "platform.membership.activated.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "platform.membership.activated.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["membershipId", "authUserId", "organizationId", "activatedAt"],
          "properties": {
            "membershipId": {"type": "string"},
            "authUserId": {"type": "string"},
            "organizationId": {"type": "string"},
            "activatedAt": {"type": "string", "format": "date-time"}
          }
        }
      }
    }
  ]
}
