{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/platform.membership.invited.v1.schema.json",
  "title": "platform.membership.invited.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "platform.membership.invited.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["invitationId", "organizationId", "inviteeReference", "expiresAt"],
          "properties": {
            "invitationId": {"type": "string"},
            "organizationId": {"type": "string"},
            "inviteeReference": {"type": "string", "description": "Opaque non-contact reference; email and credentials are excluded."},
            "expiresAt": {"type": "string", "format": "date-time"}
          }
        }
      }
    }
  ]
}
