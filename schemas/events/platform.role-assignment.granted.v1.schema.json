{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.invalid/events/platform.role-assignment.granted.v1.schema.json",
  "title": "platform.role-assignment.granted.v1",
  "allOf": [
    {"$ref": "./event-envelope-v1.schema.json"},
    {
      "type": "object",
      "properties": {
        "name": {"const": "platform.role-assignment.granted.v1"},
        "data": {
          "type": "object",
          "additionalProperties": false,
          "required": ["roleAssignmentId", "membershipId", "roleId", "scopeType", "startsAt"],
          "properties": {
            "roleAssignmentId": {"type": "string"},
            "membershipId": {"type": "string"},
            "roleId": {"type": "string"},
            "scopeType": {"type": "string", "enum": ["Tenant", "Organization", "LegalEntity", "Branch", "Location"]},
            "scopeId": {"type": ["string", "null"]},
            "startsAt": {"type": "string", "format": "date-time"},
            "endsAt": {"type": ["string", "null"], "format": "date-time"}
          }
        }
      }
    }
  ]
}
